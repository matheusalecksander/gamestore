import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    width: '100%'
  },
  content: {
    width: '100%',
    height: '50%'
  }
})

export default styles